export function OutlinedIcon({ children }: { children: string }) {

	return (
		<span className="material-symbols-outlined">{children}</span>
	);

}

export function RoundedIcon({ children }: { children: string }) {

	return (
		<span className="material-symbols-rounded">{children}</span>
	);

}

export function SharpIcon({ children }: { children: string }) {

	return (
		<span className="material-symbols-sharp">{children}</span>
	);

}
